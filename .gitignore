# Build output — lives only on the release branch
templates.json

.vscode/
Thumbs.db
.claude/

# SOPS age key (never commit private keys)
*.agekey
.sops-age-key

# Ansible vault password file (legacy, if present)
ansible/.vault-password

test

.devcontainer/linux/devcontainer-lock.json

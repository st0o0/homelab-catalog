# Base compose — logging defaults and shared config.
# Layer compose.alloy.yml and/or compose.hawser.yml on top.
x-logging: &default-logging
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"
